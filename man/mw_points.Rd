% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mw_points.R
\name{mw_points}
\alias{mw_points}
\title{Add Point Locations to Malawi Map}
\usage{
mw_points(
  data,
  lon,
  lat,
  color = "red",
  size = 2,
  shape = 19,
  alpha = 0.8,
  stroke = 0.5,
  mapping = NULL,
  show.legend = TRUE,
  jitter = FALSE,
  jitter_width = 0.1,
  jitter_height = 0.1,
  label = FALSE,
  label_column = NULL,
  label_size = 3,
  label_color = "black",
  repel_labels = FALSE,
  ...
)
}
\arguments{
\item{data}{Data frame containing coordinates and optional attributes.}

\item{lon}{Longitude column (unquoted).}

\item{lat}{Latitude column (unquoted).}

\item{color}{Point color. Can be a single color or a column name for
color mapping. Default: "red".}

\item{size}{Point size. Can be a single value or a column name for
size mapping. Default: 2.}

\item{shape}{Point shape. Default: 19 (filled circle).}

\item{alpha}{Point transparency (0-1). Default: 0.8.}

\item{stroke}{Border thickness for points. Default: 0.5.}

\item{mapping}{Optional aesthetic mapping created with \code{\link[ggplot2:aes]{ggplot2::aes()}}.}

\item{show.legend}{Logical. Show legend? Default: TRUE.}

\item{jitter}{Logical. Add small random noise to points to reduce overplotting.
Default: FALSE.}

\item{jitter_width}{Numeric. Width of jitter. Default: 0.1.}

\item{jitter_height}{Numeric. Height of jitter. Default: 0.1.}

\item{label}{Logical. Add labels to points. Default: FALSE.}

\item{label_column}{Column for point labels (if \code{label = TRUE}).}

\item{label_size}{Size of point labels. Default: 3.}

\item{label_color}{Color of point labels. Default: "black".}

\item{repel_labels}{Use ggrepel to avoid overlapping labels. Default: FALSE.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}} or
\code{\link[ggrepel:geom_text_repel]{ggrepel::geom_text_repel()}}.}
}
\value{
A ggplot2 layer object that can be added to a map.
}
\description{
Plot geographic points (e.g., survey clusters, health facilities, schools)
on a Malawi map with extensive customization options.
}
\examples{
\donttest{
library(ggplot2)

# Sample health facilities
facilities <- data.frame(
  name = c("Lilongwe Central Hospital", "Queen Elizabeth Central Hospital",
           "Mzuzu Central Hospital", "Zomba Central Hospital"),
  lon = c(33.78, 35.00, 34.02, 35.32),
  lat = c(-13.98, -15.78, -11.46, -15.38),
  type = c("Central", "Central", "Central", "Central"),
  beds = c(1200, 1350, 600, 450)
)

# Basic points
mw_map() + 
  mw_points(facilities, lon, lat)

# Colored by type (pass column name as a string)
mw_map() + 
  mw_points(facilities, lon, lat, color = "type")

# Sized by beds (pass column name as a string)
mw_map() + 
  mw_points(facilities, lon, lat, size = "beds", alpha = 0.7)

# With labels
mw_map() + 
  mw_points(facilities, lon, lat,
            label = TRUE, label_column = "name")

# Custom styling
mw_map() + 
  mw_points(facilities, lon, lat,
            color = "darkblue", shape = 16, size = 4, alpha = 0.6)
}

}
