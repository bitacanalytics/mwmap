% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/labels.R
\name{mw_labels}
\alias{mw_labels}
\title{Add District Labels to Malawi Map}
\usage{
mw_labels(
  districts = NULL,
  size = 3,
  color = "black",
  fontface = "bold",
  family = "",
  alpha = 1,
  angle = 0,
  hjust = 0.5,
  vjust = 0.5,
  check_overlap = TRUE,
  show.legend = FALSE,
  data = NULL,
  label_column = "ADM2_EN",
  ...
)
}
\arguments{
\item{districts}{Character vector of district names to label. If NULL,
labels all districts. Default: NULL.}

\item{size}{Numeric. Text size in points. Default: 3.}

\item{color}{Character. Text colour. Default: "black".}

\item{fontface}{Character or numeric. Font style: "plain", "bold", "italic",
"bold.italic". Default: "bold".}

\item{family}{Character. Font family. Default: "" (system default).}

\item{alpha}{Numeric. Text transparency (0-1). Default: 1.}

\item{angle}{Numeric. Text rotation angle in degrees. Default: 0.}

\item{hjust}{Numeric. Horizontal justification (0-1). Default: 0.5.}

\item{vjust}{Numeric. Vertical justification (0-1). Default: 0.5.}

\item{check_overlap}{Logical. If TRUE, prevents overlapping labels. Default: TRUE.}

\item{show.legend}{Logical. Include in legend? Default: FALSE.}

\item{data}{Optional sf object. If NULL, uses mw_level_2.}

\item{label_column}{Character. Column name containing labels. Default: "ADM2_EN".}

\item{...}{Additional arguments passed to \code{\link[ggplot2:ggsf]{ggplot2::geom_sf_text()}}.}
}
\value{
A ggplot2 layer object that can be added to a map.
}
\description{
Adds text labels for districts to a Malawi map. Labels are positioned at
district centroids with options for customization.
}
\examples{
\donttest{
library(ggplot2)

# Basic usage
mw_map() + mw_labels()

# Customized labels
mw_map() +
  mw_labels(size = 4, color = "darkblue", fontface = "italic")

# Labels for specific districts only
library(dplyr)
selected_districts <- mw_level_2 \%>\%
  filter(ADM2_EN \%in\% c("Lilongwe", "Blantyre", "Mzuzu City"))

mw_map() +
  mw_labels(data = selected_districts, color = "red", size = 5)
}

}
